import styled from 'styled-components';

const CloseButton = styled.button`
  /* background: black;
  color: white; */
  font-size: 1.5rem;
  border: 0;
  position: absolute;
  z-index: 2;
  right: 0;
  padding-right: 30px;
  align-self: end;
`;

export default CloseButton;
